{
  "name": "verifiableCredentials",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ Hmac512/verifiableCredentials",
    "create-local": "graph create --node http://localhost:8020/ Hmac512/verifiableCredentials",
    "remove-local": "graph remove --node http://localhost:8020/ Hmac512/verifiableCredentials",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 Hmac512/verifiableCredentials"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.30.4",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
